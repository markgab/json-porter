{
  "name": "porter-test",
  "version": "1.0.1",
  "description": "",
  "main": "src/index.ts",
  "scripts": {
    "build": "tsc && npx webpack-cli",
    "serve": "npm run build && webpack-dev-server",
    "clean": "rmdir /S /Q lib && rmdir /S /Q dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Mark Gabriel <markgabriel@gmail.com> (https://mosswell.blogspot.com)",
  "license": "ISC",
  "devDependencies": {
    "typescript": "^3.6.4",
    "webpack": "^4.41.4",
    "webpack-cli": "^3.3.10",
    "webpack-dev-server": "^3.9.0"
  },
  "dependencies": {
    "json-porter": "^1"
  }
}
